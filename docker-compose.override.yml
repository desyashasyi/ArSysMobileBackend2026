services:
  app:
    build:
      target: dev
    environment:
      PHP_IDE_CONFIG: "serverName=stage"
    restart: unless-stopped

  nginx:
    restart: unless-stopped
    ports:
      - "8000:8080"

  # --- UBAH KE MYSQL ---
  mysql:
    restart: unless-stopped
    ports:
      - "3307:3307" # Akses dari DBeaver/Laptop pakai port 3307
  # ---------------------

  redis:
    restart: unless-stopped
    ports:
      - "6381:6379"
